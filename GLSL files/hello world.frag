#ifdef GL_ES
precision lowp float;
// precision mediump float;
// precision highp float;
#endif

uniform float u_time;

void main() {
	gl_FragColor = vec4(0.68, 0.80, 0.45, 1.00);

}

// data types
// float, vec2, vec3, vec4, mat2, mat3, mat4, sampler2D and samplerCube

// math funcs
// sin(), cos(),
// tan(), asin(),
// acos(), atan(),
// pow(), exp(),
// log(), sqrt(),
// abs(), sign(),
// floor(), ceil(),
// fract(), mod(),
// min(), max()
// and clamp()
